# Firebase credentials

Do NOT commit service account keys here.

Download your key from Firebase Console → Project Settings → Service Accounts
and save it as `sec.json` in this folder. It is excluded from git via `.gitignore`.
